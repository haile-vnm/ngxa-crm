import { CanItDirective } from './can-it.directive';

describe('CanItDirective', () => {
  it('should create an instance', () => {
    // const directive = new CanItDirective();
    // expect(directive).toBeTruthy();
  });
});
